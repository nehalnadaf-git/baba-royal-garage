// ShutterSlats is no longer used — the real shutter photos handle the animation.
// Kept as an empty shell to avoid import errors from any legacy references.
export default function ShutterSlats() {
  return null;
}
